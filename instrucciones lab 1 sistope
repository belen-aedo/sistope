extensiòn es .sh

ejemplo basico en bash:
sudo echo 'hello' -> guardar en example.sh

comando por consola 
bash example.sh

ps y awk 

se deben hacer scrips 

generador = esjecutar comando de bash ps, buscar que hace cada cosa

filtrar = debemos filtrar cual es la cpu minima

en la consulta se debe colocar | lo cual significa los pipes

no se pueden usar break, formato .zip para entrega  maximo de entrega los jueves a las 23:59

//entrada:
//salida:
//descripcion: 
en cada subrutinas

 Ejecutar ps y añadir timestamp a cada línea
    # ps -eo: formato personalizado de salida
    # pid=: ID del proceso sin header
    # uid=: ID del usuario sin header  
    # comm=: nombre del comando sin header
    # pcpu=: porcentaje de CPU sin header
    # pmem=: porcentaje de memoria sin header
    # --sort=-%cpu: ordenar por CPU descendente
